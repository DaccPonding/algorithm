/**
 * 395. 至少有 K 个重复字符的最长子串
 * @param {string} s
 * @param {number} k
 * @return {number}
 */
var longestSubstring = function(s, k) {
  let len = s.length
  let left = 0, right = 0

  for (let i = 0; i < len; i++) {
     
  }
};

// console.log(longestSubstring('aaabnbb', 3));
console.log(longestSubstring('aaabnbb', 2));